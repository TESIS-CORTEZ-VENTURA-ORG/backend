# Copy to .env and fill in. .env is gitignored.

# PostgreSQL 17 LOCAL para desarrollo (docker-compose.yml → pgvector/pgvector:pg17).
# Levanta la DB con: docker compose up -d   (crea la base gastronomia_dev)
# Producción usa Neon (PG17 + pgvector): se sobreescribe esta línea al desplegar.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gastronomia_dev?schema=public"

# Puerto HTTP del backend. 3333 para NO chocar con Nuxt dev (3000);
# el BFF de Nitro proxea aquí (NUXT_API_BASE=http://localhost:3333).
PORT=3333
